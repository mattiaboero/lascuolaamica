name: CI

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  prepublish-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Ensure script is executable
        run: chmod +x prepublish-check.sh

      - name: Run prepublish checks
        run: ./prepublish-check.sh
