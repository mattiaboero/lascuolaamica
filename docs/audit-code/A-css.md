# Audit CSS — lascuolaamica.it

Perimetro: 404.css, breakout.css, faq.css, fonts.css, index.css, info-pages.css, inglese.css, noscript.css, palette-okabe.css, rewards.css, subject-quiz-theme.css, tokens.css, utilities.css.
Metodo: lettura integrale dei 13 file + grep incrociato su tutti i .html in radice e su tutti i .js (root, `js/`, `scripts/`) per verificare classi costruite dinamicamente prima di dichiarare un selettore morto. Baseline linter (`/tmp/baseline-lint.txt`) escluso dal report (unico rilievo: `#questionsTotalCount` non kebab-case in info-pages.css:423, già noto).
Vincolo rispettato: nessuna voce propone modifiche a palette-okabe.css o a regole sotto `html[data-palette="okabe-ito"]`; dove un valore duplicato ricadeva in un blocco Okabe (es. breakout.css righe 14-30, blocco "senza guardia" esplicitamente Okabe) è stato scartato, non riportato.

## Alta

1. alta | faq.html righe 39-292 (JSON-LD) davanti a faq.css:—/fonts.css | i due `<link rel="preload" as="font">` e `<link rel="stylesheet" href="fonts.css">` stanno dopo un blocco `<script type="application/ld+json">` di ~245 righe (tutte le Q&A della FAQ), mentre nelle altre pagine (es. civica.html:37-44) il blocco CSS/font-preload è compatto e il JSON-LD viene dopo | il parser deve attraversare ~250 righe di JSON prima di scoprire il font e il foglio render-blocking fonts.css, ritardando first paint e aumentando il FOUT su h1/emoji che usano Fredoka One | spostare i due preload font + `<link rel="stylesheet" href="fonts.css">` subito dopo `<link rel="stylesheet" href="faq.css">`, prima del JSON-LD.

2. alta | rewards.css (606 righe) caricato su 404.html e tabelline.html tramite js/lazy-css.js, ma verificato che questi due file NON caricano js/rewards.js e non contengono nessuna classe reward-*/lb-card/trophy-* | js/lazy-css.js inietta `rewards.css` come stylesheet reale (non solo preload) su tutte le 23 pagine indiscriminatamente; su 404.html e tabelline.html scarica/parsea ~15KB di CSS di cui 0 selettori trovano corrispondenza nel markup | condizionare l'iniezione in js/lazy-css.js (es. leggere un `data-` flag sul `<body>` o l'esistenza di `document.querySelector('[class*="reward"],.lb-card')`) invece di forzarla ovunque.

## Media

3. media | inglese.css e subject-quiz-theme.css, `@keyframes` `popIn` (inglese.css:1 / subject-quiz-theme.css:197), `slideUp` (inglese.css:38 / subject-quiz-theme.css:323), `fbPop` (inglese.css:314 / subject-quiz-theme.css:1482), `resultBoom` (inglese.css:371 / subject-quiz-theme.css:1501), `mascotResultIn` (inglese.css:385 / subject-quiz-theme.css:1515), `mascotResultCelebrate` (inglese.css:403 / subject-quiz-theme.css:1533), `emojiWiggle` (inglese.css:415 / subject-quiz-theme.css:1545) | verificato byte-per-byte identici (stesso corpo, stessa lunghezza) | inglese.html carica entrambi i file (righe 38 e 39 del link): 7 `@keyframes` completi vengono scaricati e parsati due volte, puro peso morto (l'ultima dichiarazione vince, nessun bug visivo ma spreco) | rimuovere i 7 blocchi da inglese.css e lasciare solo quelli di subject-quiz-theme.css, già caricato prima nell'head.

4. media | `.footer-support-cta` + `.footer-support-cta:hover` duplicati identici in index.css:466-487, info-pages.css:429-455, subject-quiz-theme.css:1609-1633 (anche l'override reduced-motion `.footer-support-cta:hover{transform:none;filter:none}` triplicato: index.css:548, info-pages.css:548, subject-quiz-theme.css:1802) | i tre file non vengono mai caricati sulla stessa pagina quindi non c'è doppio download runtime, ma è lo stesso identico componente (bottone CTA "supporta") copiato 3 volte: ogni modifica futura va replicata a mano in 3 posti | spostare la regola in utilities.css (già incluso da tutte e 23 le pagine) e cancellarla dagli altri tre file.

5. media | faq.css e info-pages.css non hanno il blocco `@media (prefers-reduced-motion: reduce) { *, *::before, *::after {...} } }`, presente invece in index.css:559-565, inglese.css:499-507, subject-quiz-theme.css:1817-1825 (tutti e 5 i file hanno però il rispettivo `html[data-motion="reduce"] *` per il toggle manuale del sito) | su faq.html e sulle 10 pagine info-page (accessibilita, ai-info, chi-siamo, cookie, per-genitori, per-insegnanti, privacy, supporta, supporto-satispay, tabelline) la preferenza di sistema "riduci movimento" non innesca alcuna regola CSS: l'unico meccanismo che la rispetta lì è il controllo `matchMedia` fatto via JS (es. js/faq-page.js:7, che oggi copre solo lo spawn delle emoji fluttuanti) — verificato che allo stato attuale non ci sono altre animazioni CSS autoplay su queste pagine, quindi impatto attuale limitato, ma qualunque animazione aggiunta in futuro a faq.css/info-pages.css non erediterebbe la protezione | aggiungere lo stesso blocco `@media (prefers-reduced-motion: reduce)` anche a faq.css e info-pages.css per coerenza con gli altri 3 file.

6. media | subject-quiz-theme.css:2084 `.q-explanation { ... transition: opacity 0.25s ease, max-height 0.3s ease; }` | `max-height` non è compositabile: ogni transizione forza reflow/layout, e questo elemento si apre dopo ogni risposta su tutte le 8 pagine quiz (migliaia di question/risposta nel dataset) — è quindi l'animazione a più alta frequenza d'uso del sito basata su una proprietà costosa | sostituire con `transform: scaleY()` + `transform-origin`, oppure fissare un `max-height` costante noto e animare solo `opacity`/`grid-template-rows` con `interpolate-size`, o accettare il costo (elemento piccolo) e documentarlo.

7. media | reset universale `*{box-sizing:border-box;margin:0;padding:0}` (variante testuale) ridichiarato in modo indipendente in 404.css:11, index.css:1, info-pages.css:12-16, subject-quiz-theme.css:81-85 | tokens.css è incluso da tutte le 23 pagine ma non contiene questo reset: 4 copie testuali quasi identiche invece di una sola fonte comune | spostare il reset in tokens.css una volta sola.

8. media | index.css:4-12 definisce una propria palette per le card della home (`--math-a:#9B5DE5`, `--eng-a:#006a95`, `--geo-a:#2d8f5a`, `--sto-a:#8a5a44`, `--ita-a:#d1495b`, `--civ-a:#2a9d8f`, `--prob-a:#b55914`, `--sci-a:#2a9d8f`), diversa dai token materia in tokens.css:36-44 (`--subj-math-a:#6e57a6`, `--subj-eng-a:#2e6fa6`, `--subj-geo-a:#3a8454`, `--subj-sto-a:#9a6a43`, `--subj-ita-a:#c24a5b`, `--subj-civ-a:#1f7a5e`, `--subj-prob-a:#bc5925`, `--subj-sci-a:#29837d`) usati da subject-quiz-theme.css | i colori materia sulla home (index.html) non coincidono con quelli sulle pagine quiz della stessa materia — probabile fase di migrazione Wada Sanzo non ancora completata sulla home (coerente col commento "FASE 0" in tokens.css), non verificabile con certezza se sia un lavoro in corso o una svista | se non intenzionale, far puntare `--math-a` ecc. a `var(--subj-math-a)` ecc. per allineare home e pagine quiz.

## Bassa

9. bassa | `.footer-support-cta{ color:#fff !important; }` (index.css:476, info-pages.css:439, subject-quiz-theme.css:1619) | non risulta, nei rispettivi file, nessuna regola con specificità pari o superiore che imposti il colore di un link (nessun `body.info-page a` o simile trovato) — non verificabile se un file esterno non incluso in questo audit lo richieda | `!important` sembra evitabile ma non è escludibile con certezza al 100% senza una mappa completa della cascata cross-file | provare a rimuoverlo e verificare visivamente; se non serve, toglierlo.

10. bassa | transizioni su proprietà non compositabili in pulsanti hover: index.css:264 (`transform,box-shadow`), index.css:402 (`transition:all .15s` su .cta-btn-like), index.css:485 / info-pages.css:448 / subject-quiz-theme.css:1628 (`transform,filter`), inglese.css:167,202 / subject-quiz-theme.css:550,763,833,1077,1162,1352,1365 (`transform,box-shadow[,border-color/filter]`), breakout.css:264 (`background,color`) | `box-shadow`, `filter`, `background`, `border-color` non sono compositabili: ogni hover ripaga il pixel, non solo la GPU-layer; su elementi piccoli e solo-hover l'impatto reale è basso ma è lo stesso pattern ripetuto ~15 volte | dove il box-shadow cambia solo intensità, sostituirlo con un secondo layer in `::after` animato via `opacity`, oppure accettare il costo per elementi non above-the-fold.

11. bassa | `filter: brightness(1.05)` in transizione: index.css:485-487, info-pages.css:448-455, subject-quiz-theme.css:1628-1635, inglese.css:287 (mascotte), subject-quiz-theme.css:954 (mascotte) | `filter` forza repaint dell'intero elemento (a volte compositing a parte se isolato, ma qui non è isolato con `will-change`) | aggiungere `will-change: filter` solo se il profilo prestazionale lo giustifica, altrimenti ignorare (elementi piccoli).

12. bassa | tabelline.html: JSON-LD (righe ~230-276) precede i preload font + fonts.css (righe 279-281), stesso pattern di faq.html ma su un blocco JSON-LD più corto | ritardo minore ma stesso anti-pattern di ordinamento | stesso fix: spostare font preload/fonts.css prima del JSON-LD.

13. bassa | faq.css:1-10 e subject-quiz-theme.css:1-10 ridichiarano un proprio `:root{--bg-1,--accent-1,...}` con valori hex hardcoded come fallback, poi rimappati sotto `html:not([data-palette="okabe-ito"])` ai token Wada (pattern esplicitamente commentato e intenzionale, vedi faq.css:148-158) | duplicazione voluta per fallback pre-JS/no-token, non un bug, segnalata solo per completezza | nessun fix necessario, pattern documentato nel codice stesso.

## Non verificabile

- Impatto reale a runtime dei doppioni `.footer-support-cta` (voce 4) su Core Web Vitals: richiederebbe un profiling con DevTools/Lighthouse che non rientra in un audit statico dei soli file CSS.
- Se l'`!important` di `.footer-support-cta` (voce 9) sia necessario per una regola definita in un file HTML inline o in shared.js (iniezione stile via JS): non incluso nel perimetro dei 13 file CSS richiesti.

## Selettori morti

Nessuno trovato nei 13 file. Tutte le classi apparentemente "orfane" a un primo grep statico (`.float-v2`…`.float-v20` in utilities.css) sono in realtà costruite dinamicamente via `window.SADom.randomVariant('float-v', 20)` (js/dom-utils.js:34-37, usato da subject-quiz-core.js:1401, js/index-page.js:25, js/faq-page.js:18) e quindi vive. I 4 selettori `#id` presenti nei file (`#contenuto-principale` in index.css/rewards.css/subject-quiz-theme.css, `#questionsTotalCount` in info-pages.css) sono tutti referenziati nell'HTML o in shared.js.
