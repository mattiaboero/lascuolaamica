# Audit D — js/*.js e scripts/*

Audit read-only. Nessun file modificato. Baseline lint (`/tmp/baseline-lint.txt`, solo warning `no-console` già noti) escluso dai findings sotto.

Perimetro: 14 file in `js/` (incl. `breakout.js` 53KB, `rewards.js` 29KB) + 22 script in `scripts/`. Letti anche `shared.js` e `subject-quiz-core.js` come contesto necessario (sono i file che i file `js/*-page.js` configurano/estendono).

---

## RUNTIME (js/)

| Sev. | File:riga | Cosa | Perché | Fix |
|---|---|---|---|---|
| **Alta** | `shared.js:37-107`, `subject-quiz-core.js:35-70`, `js/rewards.js:9-44`, `js/breakout.js:8-24` | Blocco `DEBUG_MODE` (IIFE hostname/`?debug`), `debugWarn`, `storageGet`, `storageSet` copiato quasi byte-per-byte in 4 file; `breakout.js` ha una variante ridotta senza `debugWarn` (errori di storage silenziati anche in debug mode) | ~29 righe duplicate in 3 file + 14 righe nella variante di breakout.js = **~101 righe di boilerplate identico**, con drift già iniziato (breakout.js non logga più gli errori come gli altri tre) | Estrarre in un file tipo `js/storage-utils.js` (stesso pattern di `js/dom-utils.js`, espone `SA.storage.get/set` + `SA.debugWarn`). Nota: oggi `shared.js` è caricato **dopo** gli script pagina in `matematica.html`/`premi.html` (`defer`, ordine da tag), quindi il nuovo file va referenziato per primo in ogni `<script>` list, non basta aggiungerlo a `shared.js` |
| **Media** | `js/dom-utils.js:6-16,24-32` | `SADom.show`, `SADom.hide`, `SADom.toggleClass`, `SADom.restartAnimation` — 4 funzioni su 6 esportate — non hanno **nessun** call-site in tutto il repo (verificato su `*.html`, `js/*.js`, `shared.js`, `subject-quiz-core.js`); anche la classe CSS `is-hidden` che `show`/`hide` gestirebbero non è mai toggled da JS (esiste solo in `utilities.css`) | Codice morto: 2/3 dell'utility file non viene mai eseguito | Rimuovere le 4 funzioni inutilizzate, o se sono pensate come API pubblica per uso futuro, documentarlo esplicitamente (altrimenti YAGNI) |
| **Media** | `js/faq-page.js:3-24` vs `js/index-page.js:3-33` | Stessa logica "crea N div decorativi con icona random e `SADom.randomVariant`, appendili a un container" duplicata (lista icone diversa, container diverso); `index-page.js` aggiunge inoltre `requestIdleCallback`/motion-check che `faq-page.js` non ha | ~15-20 righe duplicate, e le due copie sono già divergenti su reduced-motion (faq-page controlla `matchMedia` direttamente, index-page passa da `SA.motion.isReduced()` se disponibile) — prossima modifica rischia di sistemarne una sola | Estrarre `SADom.renderFloatingIcons(container, icons, count, { idle: bool })` in `dom-utils.js` |
| **Media** | `js/rewards.js:313-315` e `:369-371` | Blocco identico "se tutti i reward tranne `bacheca-piena` sono sbloccati, sblocca anche quello" ripetuto in `recordGame()` e `recordBreakout()` | Stessa condizione, stesso side-effect, copiata; se cambia la regola (es. nuovo reward escluso) va aggiornata in 2 punti | Estrarre `maybeUnlockBachecaPiena(state, unlockedNow)` |
| **Bassa** | `js/civica-page.js:4`, `js/problemi-page.js`, `js/inglese-page.js:~9` vs le altre 5 pagine materia | Chiave `subject:` duplicata a livello root di `subjectConfig` (oltre a `questionsSource.subject`, che è quella realmente usata) — presente solo in 3 file su 8; confermato via grep che `subject-quiz-core.js` non legge mai `config.subject`, solo `config.questionsSource.subject` | Chiave morta/incoerente tra file gemelli, nessun impatto funzionale ma segnala schema drift | Rimuoverla dai 3 file per uniformità, o formalizzare lo schema con un commento |
| **Bassa** | `js/index-page.js:3-16` | Uniche variabili top-level (`items`, `cont`, `isMotionReduced`) non wrappate in IIFE, a differenza di **ogni altro** file `*-page.js`/`faq-page.js` che usano `(function(){...})()` | Leak nello scope globale; basso rischio reale (pagina dedicata) ma inconsistente col resto della codebase | Wrappare in IIFE come gli altri file |
| **Nessuna** (verifica esplicita, non un problema) | `js/civica-page.js`, `geografia-page.js`, `inglese-page.js`, `italiano-page.js`, `matematica-page.js`, `problemi-page.js`, `scienze-page.js`, `storia-page.js` | **Non sono codice copiato**: sono oggetti di configurazione dati puri (banche di domande, aree, palette icone) consumati da `subject-quiz-core.js` via `window.SA.subjectConfig`. Zero funzioni in tutti e 8 i file (verificato con grep `function|=>|addEventListener|querySelector`) | La premessa "stesso codice copiato" non regge: la struttura a chiavi comuni (`totalQ`, `lbKey`, `areas`, `banks`, ...) è lo schema minimo necessario per dati diversi per materia — è già il pattern data-driven corretto, non serve un'astrazione ulteriore oltre a quanto già fa `subject-quiz-core.js` | Nessun fix: l'unica riga letteralmente identica è il wrapper `const __sa = window.SA = window.SA || {};` × 8 file, sotto la soglia dove estrarla conviene |

**Cose verificate e risultate pulite** (nessun finding, elencate per trasparenza):
- Nessuna query DOM dentro il game loop di `breakout.js` (`update`/`render`/`frame`, righe 723-1197): tutti i riferimenti DOM sono cachati fuori dal loop.
- `renderClassGrid()` in `breakout.js:1373` ricrea i bottoni con `replaceChildren()` prima di riattaccare i listener → nessun leak da re-render ripetuti.
- I modal/pannelli in `shared.js` (`ensurePlayWindowPanel`, `ensureInfoHubModal`, `bindModalEvents`, ecc.) sono tutti guardati con controlli idempotenti (`dataset.xBound`, `getElementById` prima di creare, `SA_FLAGS`) → nessun binding duplicato di listener sulle "ensure*" richiamabili più volte.
- Nessuna animazione via `style.top/left/width/height` in JS (solo canvas/CSS class toggle) → niente reflow forzati da JS.
- `dom-utils.js` (`SADom.lockScroll`, `SADom.randomVariant`) e `subject-quiz-core.js` sono correttamente riusati da `shared.js` e dai file pagina, non re-implementati.

---

## BUILD (scripts/)

| Sev. | File:riga | Cosa | Perché | Fix |
|---|---|---|---|---|
| **Alta** | `prepublish-check.sh:9-27` vs `scripts/refresh_structured_data.py:19-36` vs `scripts/generate_sitemap.py:13-35` | Tre liste hardcoded e indipendenti delle pagine HTML del sito. **`tabelline.html` e `breakout.html` esistono nel repo e sono presenti in `generate_sitemap.py`, ma mancano sia da `prepublish-check.sh` sia da `refresh_structured_data.py`** | Le due pagine più recenti saltano: (1) tutti i check di `prepublish-check.sh` (integrità HTML, pattern pericolosi `eval`/`innerHTML`, `target="_blank"` senza `rel`, favicon, noscript) — check che gira in CI ad ogni PR/push; (2) l'aggiornamento JSON-LD `datePublished`/`dateModified`/publisher di `refresh_structured_data.py`. `scripts/sync_csp_hashes.py:71,90,98` invece fa `ROOT.glob("*.html")` e non ha questo problema | Sostituire le liste hardcoded con un glob `*.html` (escludendo eventuali pagine legali se serve), come già fa `sync_csp_hashes.py`, oppure centralizzare la lista in un unico file (es. `scripts/site_pages.json`) letto da tutti e tre |
| **Media** | `scripts/ingest_generated.py:82-93` | Nessuna verifica di shard già ingeriti né dedup: rilanciare lo script sullo stesso file `.jsonl` in `reports/generated/` duplica silenziosamente le domande nel JSON materia | Non idempotente; il rischio è già noto e documentato in `docs/wiki/Contenuti-e-Domande.md` ("ingest_generated.py non deduplica... archiviare gli shard già ingeriti") ma la mitigazione è solo procedurale (memoria umana), non nel codice | Nello script: spostare/rinominare gli shard ingeriti in una sottocartella `reports/generated/ingested/` a fine run, o mantenere un manifest degli shard già processati e fare skip automatico |
| **Media** | `scripts/dedup_questions.py`, `derive_math_difficulty.py`, `fill_math_subarea.py`, `normalize_difficulty.py`, `retag_problemi_areas.py`, `update_total_questions.py` | **6 script orfani**: nessuna voce in `package.json`, nessun riferimento in `.github/workflows/*.yml`, `prepublish-check.sh`, `README.md` o `docs/wiki/*.md` (verificato con grep mirato su tutto il repo tracciato) | Zero modo scoperto di sapere quando/se vanno rilanciati; rischio che restino build tool "storici" (one-off già usati per fix passati, es. `derive_math_difficulty.py` citato nella memoria di progetto) che nessuno ricorda più come invocare correttamente | Se sono strumenti manuali validi, aggiungere una riga in `docs/wiki/Contenuti-e-Domande.md` con quando/come usarli (come già fatto per `append_parametric_pilot.py` ed `export_*.sh`); se sono obsoleti, rimuoverli |
| **Bassa** | `scripts/coverage_report.js`, `scripts/subject_quiz_test_harness.js` | Presenti come script npm (`coverage`, `test:harness`) ma non richiamati da nessun workflow CI né da `verify`/`prepublish` | Non sono "morti" (hanno un entry point npm documentato), ma la loro copertura non è enforced automaticamente — se rompono, nessuno se ne accorge finché non li lancia a mano | Nessun fix obbligato; se devono essere gate di qualità, aggiungerli a `verify` o a un workflow |
| **Bassa** | `scripts/refresh_structured_data.py:151`, `scripts/generate_sitemap.py:81` | `except Exception: return/pass` silenzioso (fallback rispettivamente su "non toccare il blocco JSON-LD" e "usa mtime file invece di git log") | Comportamento di fallback ragionevole e deliberato in entrambi i casi (non è un bug), ma nessun log neanche in caso di errore imprevisto — se il fallback scatta per un motivo diverso da quello atteso, non se ne accorge nessuno | `print(f"[warn] ...", file=sys.stderr)` nel branch except, costo minimo |
| **Bassa** | 11 script (`append_parametric_pilot.py`, `dedup_questions.py`, `derive_math_difficulty.py`, `fill_math_subarea.py`, `generate_sitemap.py`, `ingest_generated.py`, `normalize_difficulty.py`, `refresh_structured_data.py`, `retag_problemi_areas.py`, `sync_csp_hashes.py`, `update_total_questions.py`) | Scrivono direttamente su `json/*.json`/`*.html`/`sitemap.xml` senza backup esplicito | Rete di sicurezza è solo git (repo versionato, CI su ogni push) — accettabile per questo progetto, non un vero gap dato il workflow esistente | Nessun fix necessario finché il workflow resta "commit prima di lanciare script distruttivi"; eventualmente un `--dry-run` su quelli che non ce l'hanno già (`ingest_generated.py` ce l'ha già) |

**Cose verificate e risultate pulite**:
- `export_backup_outside_repo.sh` ed `export_for_cloudflare.sh`: non in `package.json` ma documentati in `README.md` e `docs/wiki/*.md`, e concatenati correttamente (`export_backup_outside_repo.sh:9` chiama `export_for_cloudflare.sh`) — non orfani.
- Ordine `freshness` (`refresh_structured_data.py && generate_sitemap.py && sync_csp_hashes.py`): coerente, non fragile — `sync_csp_hashes.py` gira per ultimo apposta per ricalcolare gli hash CSP dopo eventuali modifiche HTML dello step precedente.
- Nessun bare `except:` trovato in nessuno script Python.
- `check_sw_precache.js` e `check_update_log.js`: non wired via npm ma richiamati direttamente da `prepublish-check.sh:418,424` — non orfani.

---

## Riassunto

**Conteggio:** 3 alta, 6 media, 6 bassa (RUNTIME: 1 alta, 3 media, 3 bassa · BUILD: 2 alta, 3 media, 3 bassa).

Top 5:
1. **[Alta/BUILD]** `prepublish-check.sh` e `refresh_structured_data.py` hanno liste HTML hardcoded che hanno già driftato: `tabelline.html` e `breakout.html` saltano i check di sicurezza CI e l'aggiornamento JSON-LD. Fix: glob `*.html` come fa già `sync_csp_hashes.py`.
2. **[Alta/RUNTIME]** Storage-wrapper (`DEBUG_MODE`/`debugWarn`/`storageGet`/`storageSet`) copiato in 4 file (~101 righe), con `breakout.js` già divergente (niente debug log sugli errori storage).
3. **[Media/BUILD]** `ingest_generated.py` non deduplica al ri-lancio; mitigazione solo documentale, nessun guardrail nel codice.
4. **[Media/BUILD]** 6 script Python (`dedup_questions.py`, `derive_math_difficulty.py`, `fill_math_subarea.py`, `normalize_difficulty.py`, `retag_problemi_areas.py`, `update_total_questions.py`) sono orfani: zero riferimenti in package.json/CI/docs.
5. **[Media/RUNTIME]** `js/dom-utils.js`: 4 funzioni su 6 (`show`, `hide`, `toggleClass`, `restartAnimation`) senza alcun call-site nel repo — codice morto confermato.

Nota: la premessa "gli 8 file `*-page.js` sono codice copiato" **non è confermata** — sono config dati puri senza funzioni, già nel pattern corretto per il loro scopo.
